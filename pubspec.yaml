name: mirror
description: Cross-platform Flutter app that renders visible placeholder content.
publish_to: "none"
version: 0.1.1+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  camera: ^0.12.0+1
  camera_desktop: ^1.1.3
  flutter:
    sdk: flutter
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
